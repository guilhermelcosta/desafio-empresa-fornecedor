import { DataDdmmyyyyPipe } from './data-ddmmyyyy.pipe';

describe('DataDdmmyyyyPipe', () => {
  it('create an instance', () => {
    const pipe = new DataDdmmyyyyPipe();
    expect(pipe).toBeTruthy();
  });
});
